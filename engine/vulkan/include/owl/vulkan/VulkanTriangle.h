#pragma once

// M1 owns native Vulkan triangle lifecycle in this module. The public API is introduced only when
// the instance, surface, device, and swapchain ownership contracts are implemented.

namespace owl::vulkan
{
}
